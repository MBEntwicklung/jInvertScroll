/*! jInvertScroll 0.8.1 | Mit License */

(function($){'use strict';$.jInvertScroll=function(sel,options){var defaults={width:'auto',height:'auto',onScroll:function(percent){}};var config=$.extend(defaults,options);if(typeof sel==='Object'&&sel.length>0){return;}
var elements=[],longest=0,totalHeight,winHeight,winWidth;$.each(sel,function(i,val){$(val).each(function(e){var tmp={width:$(this).width(),height:$(this).height(),el:$(this)}
elements.push(tmp);if(longest<tmp.width){longest=tmp.width;}});});if(config.width=='auto'){config.width=longest;}
if(config.height=='auto'){config.height=longest;}
$('body').css('height',config.height+'px');$([document,window]).on('ready resize',function(e){totalHeight=$(document).height();winHeight=$(this).height();winWidth=$(this).width();});$(window).on('scroll resize',function(e){var currY=$(this).scrollTop();scrollTo(currY,config,elements,totalHeight,winHeight,winWidth);});$(window).on('scrollTo',function(e,currY){scrollTo(currY,config,elements,totalHeight,winHeight,winWidth);});};function scrollTo(newPosition,config,elements,totalHeight,winHeight,winWidth){var scrollPercent=(newPosition/(totalHeight-winHeight)).toFixed(4);if(typeof config.onScroll==='function'){config.onScroll.call(this,scrollPercent);}
$.each(elements,function(i,el){var pos=Math.floor((el.width-winWidth)*scrollPercent)*-1;el.el.css('left',pos);});}}(jQuery));
